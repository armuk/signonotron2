<h1>Organisations</h1>

<div class="well">
  <div class="row-fluid">
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Name</th>
          <th>Organisation Type</th>
          <th>Homepage</th>
          <th>ID (API Link)</th>
        </tr>
      </thead>
      <tbody>
        <% @organisations.each do |organisation| %>
          <tr>
            <td><%= organisation.name %></td>
            <td><%= organisation.organisation_type %></td>
            <td><%= organisation.web_url %></td>
            <td><%= organisation.api_id %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
